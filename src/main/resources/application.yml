# 수정: src/main/resources/application.yml
spring:
  # 기존 설정...
  servlet:
    multipart:
      resolve-lazily: true  # multipart/form-data CSRF 처리

server:
  error:
    whitelabel:
      enabled: false  # Whitelabel 에러 페이지 비활성화
    path: /error      # 에러 처리 경로
